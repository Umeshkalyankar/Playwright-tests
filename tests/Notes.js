Built In Locators:

1. page.getByRole() :  to locate by explicit and implicit accessibility attributes  mostly for input box

2. page.getByAltTest = attribute used is alt > mostly for Images/logo this attribute used

3. page.getByPlaceholder = attribute used is placeholder > to locate an element by placeholder 